char *foreav(char **a)
{
    threeav(char **a);
    if (b[1] < a[1])
        push_b();
    if (b[1] > a[1] && b[1] < a[2])
    {
        ra();
        push_b
        rra();
    }
    if (b[1] > a[2] && b[1] < a[3])
    {
        rra();
        push_b;
        ra();
    }
    if(b[1] > a[3])
    {
        push_b
        ra();
    }
    return (a);
}